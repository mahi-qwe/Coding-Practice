// Detect Capital (Easy)

var detectCapitalUse = function(word) {
    if((word === word.toUpperCase() && word[0] === word[0].toUpperCase()) || word === word.toLowerCase()){
        return true;
    }
    else{
        return false;
    }
};
