// fibinocci

package main
import "fmt"

func main() {
	var n int

	fmt.Print("Enter how many terms: ")
	fmt.Scanln(&n)

	a, b := 0, 1

	fmt.Println("Fibonacci Series:")

	for i := 0; i < n; i++ {
		fmt.Print(a, " ")
		a, b = b, a+b
	}
}
