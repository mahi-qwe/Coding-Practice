// Check If Two String Arrays are Equivalent (Easy)

var arrayStringsAreEqual = function(word1, word2) {
    let s1 = word1.join('');
    let s2 = word2.join('');
    if(s1 === s2){
        return true
    }
    else{
        return false
    }
};
